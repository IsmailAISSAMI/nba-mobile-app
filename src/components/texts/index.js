import styled from 'styled-components'

const CommonText = styled.Text`
  font-size: 24px;
`

const HeaderText = styled.Text`
  padding: 6px 12px;
  font-size: 24px;
  font-weight: 700;
  color: #1c1917;
  text-align: center;
  letter-spacing: 1px;
`

const SubHeaderText = styled.Text`
  padding: 6px 12px;
  font-size: 18px;
  font-weight: 700;
  letter-spacing: 1px;
  color: #1c1917;
`

const PrimaryButtonText = styled.Text`
  font-size: 18px;
  font-weight: 700;
  color: #fff;
`

const StatsTitle = styled.Text`
  font-size: 18px;
  padding: 10px 0; 
  font-weight: 700;
  color: #fff;
  text-align: center;
`

export { CommonText, HeaderText, SubHeaderText, StatsTitle, PrimaryButtonText }
