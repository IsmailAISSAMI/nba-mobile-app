import React from 'react'
import styled from 'styled-components'

const Login = ({ navigation }) => {
  return <TextStyled>Login</TextStyled>
}

const TextStyled = styled.Text``

export default Login
